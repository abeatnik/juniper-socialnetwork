export interface User {
    first: string;
    last: string;
    id: string;
    url: string;
    bio: string;
}

export interface UserRegistration {
    first : string;
    last : string;
    password : string;
    email : string;
}

